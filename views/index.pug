extends layout

block content
    .w3-container
        if locals.user
            h4 You are logged in!
            ul
                each val in wow
                    li= val.text
        else
            h4 You are not logged in! Please #[a(href="/login") Log In] to continue.
